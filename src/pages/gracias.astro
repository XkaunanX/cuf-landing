---
import Layout from '../layouts/Layout.astro';
import { CheckCircle, ArrowLeft } from 'lucide-react';
---

<Layout>
    <main class="h-screen flex-1 flex items-center justify-center">
      <div class="w-full max-w-md px-4 text-center space-y-6">
        <div class="flex justify-center">
          <div class="bg-green-100 p-4 rounded-full">
            <CheckCircle class="h-16 w-16 text-green-600" />
          </div>
        </div>
        <h1 class="text-3xl font-bold tracking-tighter">¡Mensaje Enviado!</h1>
        <div class="bg-blue-50 p-6 rounded-lg border border-blue-100">
          <p class="text-gray-700 mb-4">
            Gracias por contactarnos. Hemos recibido tu mensaje y nos pondremos en contacto contigo lo antes posible.
          </p>
          <p class="text-gray-600 text-sm">
            Solemos responder en un plazo de 24-48 horas hábiles.
          </p>
        </div>
        <div class="space-y-4">
          <a
            href="/"
            class="inline-flex items-center justify-center gap-2 text-primary-foreground h-10 px-4 py-2 bg-secondary hover:bg-secondary/90 rounded-md text-sm font-medium transition-colors mx-auto"
          >
            <ArrowLeft class="h-4 w-4" /> Volver a la página principal
          </a>
          <p class="text-sm text-gray-500">
            Si tienes alguna consulta urgente, puedes llamarnos al
            <a href="tel:+5411224445272" class="text-secondary hover:underline">
              +54 11 2244-445272
            </a>
          </p>
        </div>
      </div>
    </main>
</Layout>
